# 实现计划：A股动态仓位管理系统

> 基于偏移率分布网格的仓位管理系统实现路线图  
> 创建日期：2026-01-22

---

## 总体策略

**原则**：自底向上，分阶段实现，每个阶段可独立测试

**顺序**：基础设施 → 数据层 → 核心算法 → 决策模块 → 执行层 → 可视化

---

## 阶段一：基础设施搭建

### 目标
建立项目基础框架，完成配置和工具系统

### 任务清单

- [x] 1.1 项目结构初始化
  - 创建目录结构（按架构文档）
  - 初始化 `__init__.py` 文件
  - 创建 `.gitignore` 文件

- [x] 1.2 配置系统（`src/utils/config.py`）
  - 实现 YAML 配置加载
  - 配置验证和默认值处理
  - 环境变量支持

- [x] 1.3 日志系统（`src/utils/logger.py`）
  - 日志格式化配置
  - 文件日志和终端日志
  - 按模块分级别日志
  - 日志轮转

- [x] 1.4 IO 工具（`src/utils/io.py`）
  - Parquet 文件读写
  - CSV/Excel 读写
  - 目录创建和检查

- [x] 1.5 主配置文件（`config/main.yaml`）
  - Tushare API 配置
  - 偏移率计算配置
  - 分布分析配置
  - 网格划分配置
  - 仓位配置
  - 风险控制配置
  - 调仓配置
  - 报告配置

### 验收标准
- 配置文件可正常加载
- 日志可正常输出到文件和终端
- 工具函数单元测试通过

---

## 阶段二：数据获取层

### 目标
建立 Tushare 数据源封装和数据获取脚本

### 任务清单

- [x] 2.1 Tushare API 封装（`src/data_source/tushare_source.py`）
  - 初始化连接和认证
  - 日线行情获取（`get_daily()`）
  - 指数行情获取（`get_index()`）
  - 行业指数获取（`get_industry()`）
  - 股票基础信息（`get_stock_basic()`）
  - 交易日历（`get_trade_cal()`）
  - 错误处理和重试机制

- [x] 2.2 数据中心（`src/data_source/datahub.py`）
  - 统一数据入口
  - 数据缓存机制
  - 数据更新检查
  - 批量获取优化

- [x] 2.3 数据存储结构
  - 创建 `data/raw/` 目录结构
  - 创建 `data/processed/` 目录结构
  - 创建 `data/meta/` 目录结构

- [x] 2.4 数据更新脚本（`scripts/data/update_data.py`）
  - 增量更新逻辑
  - 数据完整性检查
  - 错误日志记录

### 验收标准
- 可成功获取股票日线数据
- 可成功获取指数和行业数据
- 数据正确保存到 parquet 文件
- 增量更新功能正常

---

## 阶段三：偏移率计算模块

### 目标
实现偏移率计算和滚动统计功能

### 任务清单

- [x] 3.1 偏移率计算器（`src/deviation/calculator.py`）
  - 移动均值计算
  - 移动标准差计算
  - 原始偏移率计算：`(close - ma) / ma`
  - Z-score 偏移率：`(close - ma) / std`
  - 百分位偏移率计算
  - 支持简单/指数/自适应窗口
  - 多窗口偏移率计算器

- [x] 3.2 滚动统计（`src/deviation/rolling_stats.py`）
  - 滚动均值
  - 滚动标准差
  - 滚动偏度
  - 滚动峰度
  - 滚动分位数
  - 滚动 Z-score
  - 滚动百分位排名
  - 指数加权移动统计 (EWMStats)
  - 扩展窗口统计 (ExpandingStats)

- [x] 3.3 标准化方法（`src/deviation/normalization.py`）
  - Z-score 标准化
  - Min-Max 标准化
  - 百分位标准化
  - Robust 标准化
  - 小数定标标准化
  - 滚动标准化函数
  - 工厂函数和函数式接口

- [x] 3.4 偏移率计算脚本（`scripts/analysis/calc_deviation.py`）
  - 批量计算股票偏移率
  - 批量计算指数偏移率
  - 支持多窗口计算
  - 并行处理优化
  - 结果保存到 `data/processed/deviation/`

### 验收标准
- 单个标的偏移率计算正确
- 批量计算效率满足要求
- 多种标准化方法结果正确
- 数据保存格式符合架构设计

---

## 阶段四：分布分析模块

### 目标
实现偏移率分布的统计分析功能

### 任务清单

- [x] 4.1 分布统计量计算（`src/distribution/stats_calculator.py`）
  - 均值、标准差、最小值、最大值
  - 中位数、众数
  - 偏度计算
  - 峰度计算
  - Jarque-Bera 正态性检验
  - 分位数计算（5%, 10%, 25%, 50%, 75%, 90%, 95%）

- [x] 4.2 分布形状分析（`src/distribution/shape_analyzer.py`）
  - 偏度类型判定（对称/右偏/左偏）
  - 峰度类型判定（常峰/尖峰/扁平）
  - 分布类型综合判定
  - 严重程度分级

- [x] 4.3 直方图分析（`src/distribution/histogram.py`）
  - 直方图计算
  - 分箱优化
  - 密度估计
  - 峰值检测

- [x] 4.4 核密度估计（`src/distribution/kde_estimator.py`）
  - KDE 计算
  - 带宽选择
  - 多峰检测

- [x] 4.5 分布分析脚本（`scripts/analysis/analyze_distribution.py`）
  - 批量分析股票分布
  - 生成分布统计表
  - 保存到 `data/processed/distribution/`

### 验收标准
- 统计量计算结果与标准库一致
- 分布类型判定逻辑正确
- 直方图和 KDE 可视化正确
- 批量分析性能满足要求

---

## 阶段五：网格划分模块

### 目标
实现根据分布特征动态划分网格的功能

### 任务清单

- [ ] 5.1 网格基类（`src/grid/base_grid.py`）
  - 定义网格接口
  - `generate_grid()` 抽象方法
  - `map_to_position()` 抽象方法
  - 网格配置数据结构

- [ ] 5.2 对称等距网格（`src/grid/symmetric_grid.py`）
  - 对称边界生成（基于均值和标准差）
  - 等距划分
  - 对称仓位映射（0% ~ 100%）
  - 范围倍数参数

- [ ] 5.3 非对称网格（`src/grid/asymmetric_grid.py`）
  - 根据偏度调整左右范围
  - 指数分箱边界生成
  - 非对称仓位映射
  - 右偏/左偏策略区分

- [ ] 5.4 自适应网格（`src/grid/adaptive_grid.py`）
  - 正态分布自适应网格
  - 偏态分布自适应网格
  - 重尾分布自适应网格
  - 高密度区域细分逻辑

- [ ] 5.5 区域管理器（`src/grid/zone_manager.py`）
  - 偏移率定位到区域
  - 获取对应仓位
  - 平滑过渡（边界线性插值）
  - 区域信息查询

- [ ] 5.6 网格生成脚本（`scripts/analysis/generate_grid.py`）
  - 根据分布特征选择网格类型
  - 批量生成网格配置
  - 保存到 `data/processed/grid/`

### 验收标准
- 网格边界计算正确
- 仓位映射逻辑正确
- 不同分布类型选择正确网格
- 平滑过渡功能正常

---

## 阶段六：仓位决策模块

### 目标
实现偏移率到仓位的映射和仓位计算

### 任务清单

- [ ] 6.1 偏移率-仓位映射器（`src/position/mapper.py`）
  - 封装区域管理器
  - 支持平滑/非平滑映射
  - 当前偏移率计算

- [ ] 6.2 仓位计算器（`src/position/position_calculator.py`）
  - 计算当前偏移率
  - 获取/生成网格配置
  - 映射到仓位
  - 网格缓存机制
  - 网格更新判断

- [ ] 6.3 约束管理器（`src/position/constraints.py`）
  - 最大仓位约束
  - 最小仓位约束
  - 单日最大变动约束
  - 持仓数量约束
  - 约束违反记录

- [ ] 6.4 仓位优化器（`src/position/optimizer.py`）
  - 组合层面仓位优化
  - 考虑相关性
  - 风险平价权重分配

- [ ] 6.5 仓位计算脚本（`scripts/position/calculate_position.py`）
  - 批量计算目标仓位
  - 保存到 `data/processed/position/`
  - 生成调仓建议

### 验收标准
- 单个标的仓位计算正确
- 约束条件正确应用
- 网格缓存机制正常
- 批量计算性能满足要求

---

## 阶段七：风险控制模块

### 目标
实现多层次风险控制功能

### 任务清单

- [ ] 7.1 回撤控制（`src/risk/drawdown_control.py`）
  - 组合回撤计算
  - 最大回撤监控
  - 止损线检查
  - 回撤恢复模式
  - 动态仓位缩减

- [ ] 7.2 集中度控制（`src/risk/concentration.py`）
  - 单只股票最大仓位检查
  - 单个行业最大仓位检查
  - Herfindahl 指数计算
  - 分散度评估

- [ ] 7.3 风险预警（`src/risk/alert.py`）
  - 风险指标计算
  - 预警阈值判断
  - 预警消息发送
  - 预警历史记录

- [ ] 7.4 流动性检查
  - 最小日成交额检查
  - 最大持仓比例计算
  - 调仓成本预估

### 验收标准
- 回撤计算准确
- 集中度约束正确应用
- 预警机制正常触发
- 流动性检查逻辑正确

---

## 阶段八：执行模块

### 目标
实现调仓执行相关功能

### 任务清单

- [ ] 8.1 订单生成器（`src/execution/order_generator.py`）
  - 当前持仓读取
  - 目标仓位对比
  - 买卖方向判断
  - 订单数量计算
  - 订单生成

- [ ] 8.2 成本估算（`src/execution/cost_estimator.py`）
  - 交易手续费计算
  - 滑点估算
  - 冲击成本估算
  - 总成本预估

- [ ] 8.3 调仓执行器（`src/execution/rebalancer.py`）
  - 调仓计划生成
  - 分批执行逻辑
  - 执行状态跟踪
  - 执行日志记录

- [ ] 8.4 调仓执行脚本（`scripts/position/run_rebalance.py`）
  - 读取目标仓位
  - 生成调仓计划
  - 模拟执行（或对接实盘）

### 验收标准
- 订单生成逻辑正确
- 成本估算合理
- 分批执行逻辑正确
- 调仓计划完整

---

## 阶段九：分析报告模块

### 目标
实现各类分析和可视化报告

### 任务清单

- [ ] 9.1 分布分析报告（`src/analysis/distribution_report.py`）
  - 分布直方图
  - 统计量摘要表
  - 偏度和峰度分析
  - Jarque-Bera 检验结果
  - 分布类型判定说明
  - 与理论分布对比图

- [ ] 9.2 网格可视化报告（`src/analysis/grid_report.py`）
  - 网格划分示意图
  - 当前位置标注
  - 各区域仓位分配
  - 历史分布与网格对比
  - 历史仓位变化曲线

- [ ] 9.3 仓位分析报告（`src/analysis/position_report.py`）
  - 组合仓位概览
  - 各股偏移率分布
  - 仓位配置建议
  - 风险指标汇总
  - 调仓计划列表

- [ ] 9.4 报告生成脚本（`scripts/report/generate_report.py`）
  - HTML 报告生成
  - PDF 导出
  - 报告模板管理
  - 自动化发送

### 验收标准
- HTML 报告渲染正确
- 图表清晰美观
- 数据准确无误
- 报告生成速度可接受

---

## 阶段十：测试与优化

### 目标
完善测试和性能优化

### 任务清单

- [ ] 10.1 单元测试
  - 各模块单元测试
  - 边界条件测试
  - 异常处理测试
  - 测试覆盖率 > 80%

- [ ] 10.2 集成测试
  - 端到端流程测试
  - 完整回测验证
  - 实盘模拟测试

- [ ] 10.3 性能优化
  - 数据加载优化
  - 计算效率优化
  - 内存使用优化
  - 并行处理支持

- [ ] 10.4 文档完善
  - API 文档生成
  - 使用示例编写
  - 常见问题 FAQ
  - 故障排查指南

### 验收标准
- 所有测试通过
- 性能满足要求
- 文档完整清晰

---

## 进度跟踪

| 阶段 | 状态 | 完成日期 | 备注 |
|------|------|----------|------|
| 阶段一：基础设施搭建 | ✅ 已完成 | 2026-01-22 | 通过测试 |
| 阶段二：数据获取层 | ✅ 已完成 | 2026-01-22 | 通过测试 |
| 阶段三：偏移率计算模块 | ✅ 已完成 | 2026-01-22 | 27项测试全部通过 |
| 阶段四：分布分析模块 | ✅ 已完成 | 2026-01-23 | 4项测试全部通过 |
| 阶段五：网格划分模块 | ⬜ 待开始 | - | - |
| 阶段六：仓位决策模块 | ⬜ 待开始 | - | - |
| 阶段七：风险控制模块 | ⬜ 待开始 | - | - |
| 阶段八：执行模块 | ⬜ 待开始 | - | - |
| 阶段九：分析报告模块 | ⬜ 待开始 | - | - |
| 阶段十：测试与优化 | ⬜ 待开始 | - | - |

---

## 关键里程碑

1. **M1**: 完成阶段一、二，数据可以正常获取和存储
2. **M2**: 完成阶段三、四，可以计算偏移率并分析分布特征
3. **M3**: 完成阶段五、六，可以生成网格并计算目标仓位
4. **M4**: 完成阶段七、八，可以实现完整调仓流程
5. **M5**: 完成阶段九、十，系统可用并通过测试

---

## 注意事项

1. **依赖管理**：每个阶段完成后更新 `requirements.txt`
2. **版本控制**：每个阶段完成后提交 git commit
3. **文档同步**：代码变更时同步更新架构文档
4. **配置备份**：重要配置参数的修改要记录变更原因
5. **数据备份**：原始数据和处理数据要定期备份

---

## 下一步行动

从 **阶段五：网格划分模块** 开始，实现根据分布特征动态划分网格的功能。
